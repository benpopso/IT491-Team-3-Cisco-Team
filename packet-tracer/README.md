# Packet Tracer

Store the active Packet Tracer file and recoverable checkpoints here.

Suggested filenames:

- `team3-access-layer-current.pkt`
- `checkpoint-YYYY-MM-DD-before-T-03.pkt`

Only one team member should edit the active `.pkt` file at a time. Commit a clean checkpoint before each abnormal-condition test.

